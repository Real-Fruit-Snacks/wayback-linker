# Contributing

1. Fork and clone the repository.
2. Run `npm install`.
3. Make a focused change and add or update tests.
4. Run `npm test` and `npm run build`.
5. Open a pull request describing the behavior change.

Do not commit Internet Archive access keys, secret keys, Obsidian plugin `data.json`, or vault contents.
